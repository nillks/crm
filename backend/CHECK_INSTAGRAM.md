# Проверка интеграции Instagram (Chatrace)

## ✅ Что уже сделано:

1. **Улучшена обработка webhook'ов:**
   - Добавлено детальное логирование всех входящих webhook'ов
   - Поддержка разных форматов данных от Chatrace
   - Обработка массивов сообщений и вложенных структур

2. **Добавлен polling:**
   - Автоматическая проверка новых сообщений каждые 10 секунд
   - Пробуются разные возможные endpoints Chatrace

3. **Созданы инструменты для тестирования:**
   - Скрипт `test-chatrace-webhook.sh` для тестирования webhook'ов
   - Endpoint `/instagram/check-messages` для ручной проверки

## 📋 Что нужно проверить:

### 1. Документация Chatrace

**Проблема:** В открытых источниках нет конкретной документации по webhook'ам Chatrace.

**Что делать:**
- Зайдите на https://chatrace.com/e
- Проверьте раздел "Settings" или "API"
- Ищите разделы: "Webhooks", "Callbacks", "Integrations"
- Если есть документация API, проверьте формат данных

### 2. Настройка Webhook в Chatrace

**Текущий webhook URL:**
- Локально: `http://localhost:3000/instagram/webhook` (не работает извне)
- С ngrok: `https://your-ngrok-url.ngrok.io/instagram/webhook`
- Production: `https://your-domain.com/instagram/webhook`

**Шаги:**
1. Войдите в https://chatrace.com/en/settings?acc=1543616
2. Найдите раздел "Webhooks" или "API Settings"
3. Добавьте webhook URL (для локальной разработки используйте ngrok)
4. Убедитесь, что webhook активирован
5. Выберите события: "Instagram messages" или "Incoming messages"

### 3. Проверка логов бэкенда

**Что искать в логах:**

1. **При старте сервера:**
   ```
   Instagram Service initialized (CHATRACE API)
   Chatrace API configured
   Starting Instagram message polling (checking every 10 seconds)
   ```

2. **При получении webhook:**
   ```
   ═══════════════════════════════════════════════════════
   📨 Received webhook from Instagram/Chatrace
   📅 Time: 2024-11-13T12:00:00.000Z
   📦 Body type: object
   📦 Body keys: senderId, messageId, text, ...
   📦 Full body: { ... }
   ═══════════════════════════════════════════════════════
   🔄 Processing Chatrace webhook: { ... }
   📝 Extracted data from Chatrace webhook:
     - senderId: 123456789
     - messageId: msg-123
     - text: Текст сообщения
   ✅ Chatrace Instagram message processed: ...
   ```

3. **При polling:**
   ```
   🔍 Checking for new messages: https://api.chatrace.com/messages/receive
   📦 Received response from Chatrace: { ... }
   ```

## 🧪 Тестирование

### Тест 1: Проверка webhook вручную

```bash
cd backend
./test-chatrace-webhook.sh
```

Или:

```bash
curl -X POST http://localhost:3000/instagram/webhook \
  -H "Content-Type: application/json" \
  -d '{
    "senderId": "123456789",
    "messageId": "test-123",
    "text": "Тестовое сообщение",
    "username": "test_user",
    "timestamp": 1731499200
  }'
```

**Ожидаемый результат:** В логах должно появиться полное логирование webhook'а.

### Тест 2: Проверка polling

```bash
# Проверьте логи - должно быть:
# "Starting Instagram message polling"
# "Checking for new messages: ..."
```

### Тест 3: Отправка реального сообщения

1. Отправьте сообщение в Instagram на аккаунт, подключенный к Chatrace
2. Проверьте логи бэкенда
3. Если webhook не приходит, возможно Chatrace использует только polling

## ⚠️ Возможные проблемы

### Проблема 1: Webhook не приходит

**Причины:**
- Chatrace не поддерживает webhooks (использует только polling)
- Webhook URL не настроен в Chatrace
- Сервер недоступен извне (для локальной разработки нужен ngrok)

**Решение:**
- Проверьте настройки в Chatrace
- Используйте ngrok для локальной разработки
- Проверьте логи polling - возможно, сообщения приходят через polling

### Проблема 2: Сообщения не обрабатываются

**Причины:**
- Неправильный формат данных от Chatrace
- Отсутствуют обязательные поля (senderId, text)

**Решение:**
- Проверьте логи - там будет полная структура данных
- Если формат отличается, нужно обновить код обработки

### Проблема 3: Chatrace не поддерживает получение сообщений

**Причины:**
- Сервис поддерживает только отправку сообщений
- Требуется другая настройка или другой тарифный план

**Решение:**
- Обратитесь в поддержку Chatrace
- Рассмотрите альтернативы (Instagram Graph API, другие сервисы)

## 📞 Контакты

- **Chatrace:** https://chatrace.com/en/settings?acc=1543616
- **Настройки:** https://chatrace.com/e
- **Email:** support@chatrace.com (если доступен)

## 📝 Следующие шаги

1. ✅ Проверьте настройки в Chatrace (webhook URL)
2. ✅ Отправьте тестовое сообщение в Instagram
3. ✅ Проверьте логи бэкенда
4. ✅ Если webhook не работает, проверьте polling
5. ✅ Если ничего не работает, обратитесь в поддержку Chatrace

